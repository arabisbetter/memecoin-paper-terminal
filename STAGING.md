# Staging note

A separate Supabase branch is required before applying the new evaluation migrations or deploying the new Edge Functions. Supabase reported a branch cost of $0.01344/hour. Creation is pending explicit user cost approval.

Until that approval, this branch is code-only and must not mutate the production database.
